def get_recommendation_report_prompt(recommendation_data, work_hours):
    """
    최적화 추천 데이터를 기반으로 LLM 한글 리포트를 생성하기 위한 프롬프트를 반환합니다.
    """
    import json
    return f"""
    역할: 너는 친환경 클라우드 가상화 및 탄소 효율성(Carbon-Aware Computing) 분석 전문가이다.
    작업: 아래 제공된 리전별 탄소 집약도 분석 데이터를 기반으로, 가상 머신(VM)을 배치하기에 가장 탄소 효율적인 '최적 리전'과 '최적 시작 시간대'를 추천하는 자연어 보고서를 한국어로 한 문단(약 3-4문장 내외)으로 작성하라.
    
    제약 조건:
    1. 분석 대상 리전 정보(KR, JP-TK, US-MIDA-PJM 등)의 대조적인 평균 집약도 상황을 가볍게 언급하라.
    2. 추천되는 최적 리전과 구체적인 최적 실행 시점(시작 일자 및 시간대)을 강조하여 제시하라.
    3. 평균 배출량 대비 최적 시점 배치 시의 예상 탄소 절감 배출량을 언급하라.
    4. 친절하고 신뢰감 있는 마크다운 형식의 어조를 사용할 것.
    5. 만약 두 개 이상의 리전이 동일하게 최적 탄소 집약도(opt_intensity)를 갖는 등 공동 1위가 될 경우, 공동 최적 리전으로 함께 언급하여 사용자에게 유연한 선택지를 제공하라.
    6. 데이터가 제한적이거나 동적인 변화가 부족한 경우(예: Sandbox 환경의 정적 데이터셋 상황), 이를 기반으로 한 한계점이 있을 수 있음을 리포트에 자연스럽게 안내하라.

    
    입력 데이터:
    - 작업 실행 예정 시간: {work_hours}시간
    - 최적화 스코어링 결과: {json.dumps(recommendation_data, indent=2, ensure_ascii=False)}
    
    출력 예시 형태:
    분석 결과, **{work_hours}시간** 분량의 가상화 작업을 수행하기에 가장 탄소 배출이 적은 최적 리전은 **US-MIDA-PJM**이며, 최적 시작 시간은 **[일자] [시]**로 추천됩니다. 이 시점에 스케줄링을 배치하면 리전 평균 대비 약 **[절감률]%**의 탄소를 절감할 수 있습니다. 반면, 동시간대 탄소 배출량이 상대적으로 매우 높은 **한국(KR)** 리전의 배치는 후순위로 미루는 것을 제안합니다.
    """
